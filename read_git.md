---
git checkout -b p1_setup
git add .
git commit -m "change: first"
git push origin p1_setup

---
git checkout -b p2_create_user_mail_service
git add .
git commit -m "change: first"
git push origin p2_create_user_mail_service

---
git checkout -b p3_chat_service
git add .
git commit -m "change: first"
git push origin p3_chat_service

---
git checkout -b p4_fontend
git add .
git commit -m "change: first"
git push origin p4_fontend